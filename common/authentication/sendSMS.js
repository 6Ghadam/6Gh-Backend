const utility = rootRequire('helper/utility');

module.exports = async Authentication => {

  Authentication.sendSMS = async () => {

  };

  Authentication.sendSMS = utility.wrapper(Authentication.sendSMS);

};